library(pacman) # loads and installs package if needed for use
pacman::p_load(
  accessibility,
  arrow,
  bslib,
  curl,
  dplyr,
  DT,
  duckdb,
  eeptools,
  epitools,
  epikit,
  flexdashboard,
  fontawesome,
  forcats,
  ggplot2,
  ggrepel,
  ggridges,
  ggthemes,
  glue,
  here, # manage file paths consistently
  hexbin,
  highcharter,
  htmltools,
  htmlwidgets,
  janitor, # For clean_names()
  leaflet,
  lubridate, # working with dates
  maps,
  paletteer,
  plotly, # interactive plots
  purrr,
  qs,
  RColorBrewer,
  reactable,
  readr,
  rlang,
  repurrrsive,
  rio, # import/export
  rmarkdown,
  rsconnect,
  scales, # quick percents
  sf,
  shiny,
  showtext,
  snakecase,
  sp,
  stringr,
  thematic,
  tidymodels,
  tidyverse, # data management and visualization
  tidyr,
  tigris,
  tools,
  uuid, # For UUID generation
  viridisLite,
  writexl
)

